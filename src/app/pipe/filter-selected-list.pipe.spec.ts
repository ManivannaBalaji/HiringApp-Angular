import { FilterSelectedListPipe } from './filter-selected-list.pipe';

describe('FilterSelectedListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectedListPipe();
    expect(pipe).toBeTruthy();
  });
});
